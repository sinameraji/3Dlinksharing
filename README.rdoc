== 3D Printer's Hackathon

This is a pretty simple Rails project to help us (teammates) share useful links.

Simple bootstrap UI, just to get work done. 

There is no User controller since the deployed app's link isn't shared with public. (It's an excuse, not a reason)

#LearningByDoing

